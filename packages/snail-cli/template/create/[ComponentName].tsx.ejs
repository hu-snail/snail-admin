import { defineComponent } from 'vue'
import { props } from './props'
import './<%- camelizeName %>.scss'

export default defineComponent({
  name: '<%- bigCamelizeNamespace + bigCamelizeName + createType %>',
  props,
  setup(props, { slots }) {
    return () => {
      return <div class="<%- namespace %>-<%- kebabCaseName %>-<%- type %>">{ slots.default?.() }</div>
    }
  },
})
